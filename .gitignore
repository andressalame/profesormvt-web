node_modules/
dist/
.astro/
src/env.d.ts
*.log
.DS_Store
.admin-token.local
batuta-app/.wrangler/

# respaldos temporales de una sesión de trabajo, no van al repo
*.bak-*

# Volcados reales de la D1 para las pruebas del motor (bin/fixtures.mjs los regenera).
# 🔒 NO van al repo: este repo es PUBLICO y esto es la operacion de un cliente
#    (horarios, asistencia, planes, ~1600 fichas). Van anonimizados, pero anonimizado
#    no es publicable. pruebas.sh los rehace solo si faltan. (24-ago-2026)
batuta-app/datos/fixtures/

# Maquetas del rediseño (24-ago-2026): se ven en local con `astro dev` para elegir
# dirección, pero NO deben publicarse. Viven en public/ solo para que /fonts/ resuelva.
# Antes de desplegar: borrar public/_dirs/ (o dejarlas acá, que no viajan al repo).
public/_dirs/
public/_fonttest.html
batuta-app/.wa-verify-token.local
